<%= form_with(model: adverse_reaction_type, local: true) do |form| %>
  <% if adverse_reaction_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adverse_reaction_type.errors.count, "error") %> prohibited this adverse_reaction_type from being saved:</h2>

      <ul>
      <% adverse_reaction_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
